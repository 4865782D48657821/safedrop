# Test Levels

- Unit: pure logic, validation, transformation, permissions helpers.
- Component: module behavior with local dependencies.
- Integration: database, API, queue, filesystem, or external adapter boundary.
- End-to-end: critical user workflow across major boundaries.
- Operational: migration dry run, rollback rehearsal, observability probe, load check.
