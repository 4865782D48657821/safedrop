# Implementation Principles

- Prefer existing local patterns and helper APIs.
- Make the smallest coherent change that satisfies the requirement.
- Keep behavior changes, refactors, and formatting separate when possible.
- Preserve backward compatibility unless intentionally changed and documented.
- Fail explicitly on invalid states instead of silently corrupting data.
